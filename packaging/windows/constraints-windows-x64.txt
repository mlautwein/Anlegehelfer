# Exakte Versionen des Windows-x64-Builds (pip constraints).
#
# Zweck: pyproject.toml gibt bewusst Bereiche an, damit die Testmatrix
# Regressionen in neuen Bibliotheksversionen findet. Der AUSGELIEFERTE
# Build soll dagegen ueber die Zeit stabil bleiben - deshalb loest er
# gegen diese Datei auf. Sonst haetten zwei Builds im Abstand von Monaten
# unterschiedliche Bibliotheken in derselben EXE.
#
# Erzeugt von: pip freeze --exclude-editable in einem frischen .venv-build
# auf windows-latest, Python 3.12, CI-Lauf 32973018101. Die Isolation ist
# wichtig: ohne sie stehen hier auch vorinstallierte Pakete des
# Runner-Images (pipx, filelock, ...), die unser Install nie anfasst.
#
# Aktualisieren: CI-Artefakt "constraints-windows-x64" herunterladen und
# den Block unter diesem Kopf ersetzen. Der CI-Schritt "Versionsbindung
# pruefen" schlaegt fehl, sobald installierte Versionen abweichen.
altgraph==0.17.5
annotated-types==0.8.0
cffi==2.1.1
charset-normalizer==3.5.1
colorama==0.4.6
cryptography==50.0.1
et_xmlfile==2.0.0
flatbuffers==25.12.19
iniconfig==2.3.0
numpy==2.5.2
onnxruntime==1.29.0
opencv-python==5.0.0.93
openpyxl==3.1.5
packaging==26.3
pdfminer.six==20251230
pdfplumber==0.11.9
pefile==2024.8.26
pillow==12.3.0
pillow_heif==1.5.0
pluggy==1.6.0
protobuf==7.36.0
psutil==7.2.2
pyclipper==1.4.0
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
Pygments==2.21.0
pyinstaller==6.22.2
pyinstaller-hooks-contrib==2026.7
pypdfium2==4.30.0
pytest==9.1.1
pywin32-ctypes==0.2.3
PyYAML==6.0.3
rapidocr-onnxruntime==1.4.4
reportlab==5.0.1
setuptools==84.0.0
shapely==2.1.2
six==1.17.0
tqdm==4.70.0
typing-inspection==0.4.4
typing_extensions==4.16.0
xlrd==2.0.2
xlwt==1.3.0
